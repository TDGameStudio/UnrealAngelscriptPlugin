# Cross-module binding ABI layout version.
# Bump this token when any FAngelscriptCrossModuleBinding POD field is added, removed,
# reordered, widened, narrowed, or given new semantics.
# Bump this token when the runtime reader layout and emitted cpp layout stop changing together.
0xA5C0DE02
